services:
  crossref-mcp:
    build: .
    image: crossref-mcp:latest
    stdin_open: true
    tty: false
    environment:
      - NODE_ENV=production
      - USER_AGENT=CrossRefMCP/1.0 (mailto:alberto.mercurio@epfl.ch)